server:
  port: 8081

spring:
  application:
    name: usuarios-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:azure}
  jpa:
    open-in-view: false

# Mismo nombre de propiedad y mismo header que usan pedidos-service y envios-service.
internal:
  token: ${INTERNAL_TOKEN}

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.pedidos360.usuarios: DEBUG
