# Perfil por defecto: Azure SQL. Credenciales solo por variable de entorno.
spring:
  datasource:
    url: ${USUARIOS_DB_URL}
    username: ${USUARIOS_DB_USERNAME}
    password: ${USUARIOS_DB_PASSWORD}
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: usuarios
